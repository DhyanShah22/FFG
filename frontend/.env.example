# Spring Boot API base URL (restart npm start after changing this file)
REACT_APP_API_BASE_URL=http://localhost:8080/api/v1

# Set to true only to use the in-memory mock API instead of the backend.
REACT_APP_USE_MOCK=false
